name: Deploy Resume App to GitHub Pages

on:
  push:
    branches:
      - main # Triggers the build automatically whenever you commit to main

permissions:
  contents: write # Grants permission to push the built bundle to the gh-pages branch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"

      - name: Install Dependencies
        run: npm ci

      - name: Build React Application
        run: npm run build
        env:
          CI: false

      - name: Configure SPA Routing Fallback
        run: cp dist/index.html dist/404.html # Duplicate index.html to handle client-side routing refreshes

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # Vite builds into the 'dist' folder by default
          branch: gh-pages
          clean: true
